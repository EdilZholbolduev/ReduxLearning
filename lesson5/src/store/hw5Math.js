import { createSlice } from "@reduxjs/toolkit";

const mathSlice = createSlice({
   name:'mathSlice',
   initialState:{
      input1: '5',
      input2: ''
   },
   reducers:{

   }
})



export default mathSlice.reducer;